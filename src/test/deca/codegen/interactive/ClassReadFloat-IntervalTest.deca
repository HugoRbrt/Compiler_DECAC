// Description:
//      Interactive test to know if an int given in standard input
//      is in [0, 5]. In this case, we use a class
//
// Results:
//      x is in [0, 5]       OR       x is NOT is [0,5]
//
// History:
//      created on 23/1/2021

class Reader {
	
	int x = 0;

	void verifyInterval() {
		float y = readFloat();
    		if ((x+y) >= 0 && (x+y) <= 5) {
        		println("x+y belongs to [0, 5]");
    		} else {
        		println("x+y does not belong to [0, 5]");
    		}
	}
}

{
    Reader r = new Reader();
    r.verifyInterval();
}
